# 나란 프로젝트 인수 문서

> 이 문서는 NaRan 프로젝트를 다른 개발 에이전트에서 Codex로 이어서 작업하기 위한 팀 내부 컨텍스트다.  
> 사용자에게 답변할 때는 한국어를 기본으로 사용한다.  
> 날짜 기반의 예전 계획보다 아래의 `Step` 순서와 최신 기획 문서를 우선한다.

---

## 1. 프로젝트 한 줄 정의

기업의 지속가능경영보고서에서 검증 가능한 환경 주장을 추출하고, 공적 시스템의 환경 데이터와 **비교 가능한 범위인지 먼저 검사한 뒤**, 비교 가능한 수치만 대조하여 근거와 후속 확인 질문을 금융기관 담당자에게 제공하는 AI 사후관리 보조 도구다.

제품명은 **나란**이다.

### 이름 변경에 따른 구현 원칙

- 사용자에게 보이는 제품명과 새 문서 표기는 `나란(NaRan)`을 사용한다.
- 프로젝트 폴더의 표준 경로는 `/Users/gimminji/Developer/NaRan`이다.
- Python 패키지와 import의 표준 식별자는 `naran`이다.
- 새 코드와 설정에 이전 식별자 `Daejo` 또는 `daejo`를 사용하지 않는다.

---

## 2. 이 문서의 사용법과 기준 문서

### 우선순위

내용이 충돌할 경우 다음 순서로 판단한다.

1. 사용자의 현재 대화 지시
2. 이 `claude.md`의 안전·구현 원칙
3. 최신 기획서  
   `/Users/gimminji/Documents/Codex/2026-07-28/k/outputs/기획서_최종개정본.md`
4. 최신 개발 계획  
   `/Users/gimminji/Documents/Codex/2026-07-28/k/outputs/나란_개발계획.md`
5. 시장조사·경쟁 분석  
   `/Users/gimminji/Documents/Codex/2026-07-28/k/outputs/나란_시장조사_경쟁분석.md`
6. 저장소 안의 기존 `기획서.md`, `개발계획.md`

### 중요한 주의

저장소 안의 `기획서.md`와 `개발계획.md`는 **이전 버전**이다. 다음과 같은 폐기된 설계가 포함돼 있으므로 그대로 구현하면 안 된다.

- 환경정보공개시스템 값을 일괄적으로 `법정 신고 실측치`라고 부르는 표현
- 공개 데이터 하나를 절대적인 정답지로 두는 접근
- 연차 경로가 없는데 선형 감축 궤적을 임의 생성하는 로직
- 고정 `TOLERANCE`로 일치 여부를 판정하는 방식
- `정합/궤도이탈/역행` 중심의 예전 상태 모델
- 날짜별 D1~D7 일정
- 유사 서비스가 없거나 국내 최초라고 단정하는 표현

최신 개발 계획은 날짜가 아니라 `Step 1`부터 `Step 17`까지의 선후관계로 작성돼 있다.

---

## 3. 현재 저장소 상태

현재 확인된 파일은 다음과 같다.

```text
NaRan/
├── .venv/
├── naran/
│   ├── __init__.py
│   └── contracts.py
├── db/
│   └── envinfo.py
├── fixtures/
│   ├── sample_case_a.json
│   ├── sample_case_b.json
│   └── sample_case_c.json
├── tests/
│   └── test_contracts.py
├── pyproject.toml
├── requirements-dev.txt
└── claude.md
```

현재 상태에서 주의할 점:

- 아직 Git 저장소가 아니다.
- API와 웹 애플리케이션 골격이 없다.
- 다른 Codex 세션에서 공통 계약, A·B·C fixture와 계약 테스트 개발을 시작했다.
- 감탄의 범용 코드가 실제로 이전됐다고 가정하면 안 된다.
- `.venv`가 있지만 필요한 의존성과 재현 가능성이 검증되지 않았다.
- 동시에 작업하는 세션이 있을 수 있으므로 현재 변경 중인 파일을 덮어쓰지 않는다.

Codex는 작업을 시작할 때 현재 파일 목록과 상태를 다시 확인해야 한다. 사용자의 요청 없이 기존 파일을 대량 삭제하거나 감탄 저장소 전체를 복사하지 않는다.

---

## 4. 기존 프로젝트와의 관계

선행 프로젝트 이름은 **감탄(鑑炭)**이며 경로는 다음과 같다.

```text
/Users/gimminji/Developer/GamTan
```

새 프로젝트 경로:

```text
/Users/gimminji/Developer/NaRan
```

감탄과 나란은 같은 아이디어가 아니다.

| 구분 | 감탄 | 나란 |
| --- | --- | --- |
| 핵심 입력 | 기업 증빙·활동자료 | 발행된 지속가능경영보고서와 공개 환경 데이터 |
| 핵심 문제 | Scope 1·2 배출량 산정 | 서로 다른 출처의 범위 정렬과 정합성 검토 |
| 핵심 사용자 | 배출량을 산정하는 기업 사용자 | 녹색여신 사후관리 담당자 |
| 핵심 출력 | 계산된 배출량 | 비교 가능성, 근거, 차이, 후속 확인 질문 |
| 사용하지 않을 로직 | — | 감탄의 탄소 계산, PCAF, 전표 분류 |

감탄에서 재사용할 수 있는 것은 제품 아이디어가 아니라 범용 실행 기반이다.

- 트레이스 기록 구조
- 실행 잠금과 진행 상태
- DB 연결 패턴
- Gemini 호출·캐시·재시도 패턴
- 타임라인과 실시간 트레이스 UI 뼈대
- Docker 실행 기반

다음은 가져오지 않는다.

- 탄소 계산 엔진
- PCAF 로직
- 전표·배출계수·환산단가 모델
- 홈택스·한전 Mock API
- 감탄 전용 시나리오와 화면

감탄 저장소 전체를 복사하거나 수정하지 않는다. 필요한 파일을 먼저 읽고, NaRan로 선별 이전한다. 감탄의 작업 중인 변경이나 미추적 파일을 건드리지 않는다.

---

## 5. 제품의 핵심 차별점

비슷한 기능을 가진 서비스는 이미 존재한다.

- ESG Book Engage: 고객 ESG 데이터 수집, 공개 데이터 결합, 불일치 확인
- Workiva: 기업 내부 공시 작성 과정의 이상치·불일치 탐지
- KB 탄소관리시스템: 기업의 배출량 산정·관리
- SK AX: 문구와 법령·사례의 컴플라이언스 검토
- Climate TRACE: 독립적인 배출량 관측·추정

따라서 `유사 서비스가 없다`, `최초다`라고 주장하지 않는다.

나란의 방어 가능한 차별점은 다음 결합이다.

> 한국 금융회사의 녹색여신 사후관리 시점에, 한국어 보고서의 주장과 env-info·GIR 데이터의 기업·사업장·조직경계·지역경계·Scope·Scope 2 산정 방식·기간을 먼저 맞추고, 비교 가능한 값만 계산한 뒤 근거와 후속 질문을 HITL로 연결한다.

가장 중요한 기술 장면은 숫자 차이를 찾아내는 장면이 아니라 **범위가 다른 숫자의 계산을 중단하는 장면**이다.

---

## 6. 절대 지켜야 할 도메인 원칙

### 공개 데이터는 정답지가 아니다

환경정보공개시스템과 GIR은 유용한 공개 근거지만 성격과 범위가 다르다.

- 환경정보공개시스템의 `온실가스 관리수준 및 배출량`은 자율 공개 항목이다.
- GIR은 할당대상업체·관리업체의 명세서 정보를 제공하지만 데이터가 추후 수정될 수 있다.
- 기업, 법인, 연결 기준, 개별 사업장, 국내, 글로벌 범위가 서로 다를 수 있다.
- Scope 2는 location-based, market-based, 배출권거래제 기준을 구분해야 한다.
- 같은 숫자처럼 보여도 기간, 단위, 조직경계가 다르면 비교할 수 없다.

화면과 코드 주석에서 모든 공개 데이터를 `실측치`, `법정 신고값`, `정답`으로 일반화하지 않는다.

### 모르면 계산하지 않는다

- 필수 정보가 없으면 `정보 부족`
- 필수 정보는 있지만 범위가 다르면 `비교 불가`
- 위 두 상태에서는 차이와 차이율을 계산하지 않는다.
- 이름 유사도만으로 기업과 사업장을 자동 확정하지 않는다.
- 공란이나 미공개를 숫자 0으로 바꾸지 않는다.

### LLM의 역할을 제한한다

LLM은 다음만 수행한다.

- 비정형 보고서에서 주장 후보 추출
- 주장 유형과 메타데이터 구조화
- 원문과 페이지 근거 연결
- 차이 원인의 설명이 문서에 실제로 있는지 보조 확인

LLM은 다음을 수행하지 않는다.

- 최종 분석 상태 생성
- 차이율 계산
- 비교 가능성 규칙 우회
- 그린워싱·허위·위반 판정
- 금리·한도·신용등급 결정

### 계산은 결정론적으로 한다

- 비교 가능성 검사는 순수 함수로 구현한다.
- 차이와 차이율은 비교 가능한 경우에만 계산한다.
- 원본값과 정규화값을 모두 보존한다.
- 고정 허용오차를 두지 않는다.
- 두 출처 중 더 낮은 표시 정밀도와 확인된 표시 규칙을 사용한다.
- 표시 규칙이 확인되지 않으면 임의로 `반올림`이라고 단정하지 않는다.
- 연차별 감축 경로가 공개되지 않았으면 선형 경로를 생성하지 않는다.

### 사람의 판단을 보존한다

- AI는 플래그와 근거를 제공한다.
- 최종 업무 판단은 담당자가 한다.
- `Verdict`와 `HumanReview`는 분리 저장한다.
- 담당자 조치가 AI 분석 상태를 덮어쓰지 않는다.
- 담당자 조치 변경은 append-only 감사 이력으로 남긴다.

---

## 7. MVP 지원 범위

### 지원 주장

- `감축목표`
- `실적주장`

### 범위 밖 주장

- `전환주장`: 외부 전력·인증 데이터 필요
- `인증주장`: 외부 인증기관 조회 필요
- `정성주장`: 정량 대조 대상 아님

범위 밖 주장을 억지로 판정하지 않는다.

### 실적주장 분석 상태

다음 여섯 상태만 사용한다.

1. `일치`
2. `설명된 차이`
3. `설명 가능성 있음`
4. `설명되지 않은 차이`
5. `비교 불가`
6. `정보 부족`

`감축목표`는 이 여섯 상태와 섞지 않고 `목표 추적 준비도`로 관리한다.

### 기본 검토 라우팅

| 상태 | 기본 `review_required` |
| --- | --- |
| 일치 | `false` |
| 설명된 차이 | `false` |
| 설명 가능성 있음 | `true` |
| 설명되지 않은 차이 | `true` |
| 비교 불가 | `true` |
| 정보 부족 | `true` |

단, `match_type=precision_compatible`인 일치는 대표 상태를 `일치`로
유지하면서, 출처의 표시 규칙이 확인되지 않은 경우에 한해
`review_required=true`로 후속 확인을 요청한다.

---

## 8. 반드시 재현해야 하는 세 사례

### A — 삼성바이오로직스: 일치

- 실제 기업 공개자료 사용
- 2024년 국내 사업장
- Scope 1: 보고서 71,840.290tCO₂eq / env-info 71,840tCO₂eq
- Scope 2: 보고서 154,678.989tCO₂eq / env-info 154,679tCO₂eq
- Scope 2 산정 방식은 `배출권거래제 기준`
- 합계: 보고서·env-info·GIR 모두 226,519tCO₂eq
- 개별값은 `표시 정밀도 범위 내 정합`
- 합계는 정수 표시값 기준 일치
- 보고서 검증의견서와 GIR 검증수행기관 정보도 근거로 연결

기대 결과:

```text
status = 일치
match_type = exact 또는 precision_compatible
```

### B — 삼성전자: 비교 불가

- 실제 기업 공개자료 사용
- 보고서 데이터: 국내외 제조사업장을 포함하는 글로벌 전사 범위
- env-info 데이터: 수원·기흥·화성·평택·구미 등 국내 개별 사업장
- 글로벌 전사 수치와 특정 국내 사업장 수치를 직접 비교하지 않는다.

기대 결과:

```text
status = 비교 불가
차이 계산 = 실행하지 않음
다음 행동 = 동일한 조직·지역 범위 자료 요청
```

공식 데모의 첫 진입 사례로 적합하다. 이 사례에서 `잘못된 비교를 중단하는 능력`을 보여준다.

### C — 가상기업 C: 설명되지 않은 차이

- 실재 기업과 무관한 합성 사례
- 2024년 국내 사업장
- Scope 1+2
- 배출권거래제 기준
- 보고서 주장: 100,000tCO₂eq
- 합성 공개 데이터 fixture: 125,000tCO₂eq
- 조직·지역·Scope·기간·산정 방식은 모두 일치
- 절대 차이: 25,000tCO₂eq
- 보고서 주장값 대비 차이율: 25%
- 차이를 설명하는 근거는 없음

기대 결과:

```text
status = 설명되지 않은 차이
review_required = true
monitoring_data_synthetic = true
```

질문 초안:

```text
동일한 2024년 국내 사업장 Scope 1+2 배출량 간
25,000tCO₂eq 차이가 발생한 원인과 산정 근거를 제출해 주세요.
```

C의 25%는 위험점수나 위법 가능성이 아니라 두 합성 수치의 결정론적 차이율이다.

---

## 9. 목표 사용자 흐름

```text
사후관리 검토 대기열
→ 새 보고서가 연결된 관리 건 선택
→ 보고서·기업·합성 여신 정보 확인
→ 분석 시작
→ 주장 추출
→ 공개 데이터 조회
→ 비교 가능성 검사
→ 비교 가능한 값만 대조
→ 원문·페이지·출처·계산 근거 확인
→ 추가 자료 요청 / 검토 완료 / 보류
→ 트레이스와 감사 이력 확인
```

첫 화면은 대규모 통계 대시보드가 아니라 A·B·C 세 건의 작은 사후관리 대기열이다.

대기열 정렬:

1. `review_required=true`
2. 다음 점검일이 가까운 순
3. 합성 중요도 등급

중요도는 AI 위험점수가 아니다.

대출액, 금리, 신용등급은 MVP 스키마와 화면에 넣지 않는다.

---

## 10. 핵심 데이터 계약

구현 전에 백엔드 Pydantic 모델과 프론트 타입을 함께 고정한다.

| 객체 | 필수 내용 |
| --- | --- |
| `Company` | 기업 ID, 법인명, 식별자, 별칭 |
| `Report` | 기업, 제목, 보고연도, 파일 해시, 출처 |
| `Claim` | 유형, 값, 단위, 기간, Scope, 산정 방식, 조직·지역경계, 원문, 페이지, confidence |
| `PublicFact` | 기업·사업장 ID, 값, 단위, 기간, Scope, 산정 방식, 조직·지역경계, 출처, 조회일, 해시, 버전 |
| `ComparabilityResult` | 조건별 결과, 누락 항목, 불일치 이유, 비교 실행 가능 여부 |
| `Verdict` | 여섯 상태, match type, 원본값, 정규화값, 차이, 설명, review 정보, 질문 |
| `TargetProgress` | 준비도, 기준값, 현재값, 실제 감축률, 연차 경로, 선택적 계획 대비 차이 |
| `TraceEvent` | 계획·관찰·행동, 도구, 입력 요약, 근거, 시각 |
| `HumanReview` | 담당자 조치, 메모, 검토자, 처리 시각, 변경 이력 |
| `MonitoringCase` | 관리 건 ID, 유형, 다음 점검일, 중요도, `monitoring_data_synthetic=true` |

필수 규칙:

- 상태, Scope, 산정 방식, 조직경계는 enum을 사용한다.
- `0`, 공란, 미공개, 미명시를 구분한다.
- 모든 수치에 단위와 기간을 붙인다.
- 원본값을 정규화값으로 덮어쓰지 않는다.
- 공개 데이터는 URL·조회일·해시·버전을 갖는다.
- LLM 스키마에는 `Verdict` 필드를 넣지 않는다.

---

## 11. 목표 모듈

### 새로 만들 핵심 모듈

| 모듈 | 책임 |
| --- | --- |
| `db/parser.py` | PDF를 페이지별 텍스트·표로 추출하고 원문 위치 보존 |
| `api/agent/llm_extract.py` | 주장 구조화, live/demo 모드, Pydantic 검증 |
| `db/public_data.py` | env-info·GIR 데이터를 `PublicFact`로 정규화 |
| `db/public_snapshot.py` | 출처·조회일·원본 해시·버전과 갱신/fallback 관리 |
| `db/entity_map.py` | 법인과 대표·개별 사업장 식별자 매핑 |
| `db/comparability.py` | 지표·단위·조직·지역·Scope·방식·기간 검사 |
| `db/compare_engine.py` | 비교 가능한 수치만 차이·차이율 계산 |
| `api/agent/review_difference.py` | 조직변동·재산정·산정 방식 변경 등 설명 근거 확인 |
| `api/agent/orchestrator.py` | 추출 결과와 검사 결과에 따른 다음 도구 선택 |
| `SceneQueue.tsx` | 사후관리 대기열 |
| `SceneClaims.tsx` | 주장과 원문 근거 |
| `SceneCompare.tsx` | 비교 조건과 대조 결과 |
| HITL 화면 | 담당자 조치·메모·감사 이력 |

### 권장 API

| 방식 | 경로 | 목적 |
| --- | --- | --- |
| `GET` | `/cases` | 사후관리 대기열 |
| `GET` | `/cases/{id}` | 사례·보고서·현재 상태 |
| `POST` | `/cases/{id}/analyze` | 분석 실행 |
| `GET` | `/runs/{id}` | 실행 상태 |
| `GET` | `/runs/{id}/trace` | 트레이스 |
| `GET` | `/claims/{id}` | 주장·근거·비교 결과 |
| `POST` | `/reviews` | 담당자 조치 추가 |
| `GET` | `/reviews/{case_id}/history` | 감사 이력 |
| `POST` | `/public-data/refresh` | 공개 데이터 수동 갱신 |

API 경로는 구현 전에 프론트와 백엔드가 함께 확인하고, 이미 더 적절한 구조가 있으면 이유를 기록한 뒤 조정할 수 있다.

---

## 12. 비교 가능성 검사

숫자 계산보다 먼저 다음 항목을 검사한다.

1. 지표
2. 단위
3. 절대량/원단위
4. 조직경계
5. 지역경계
6. Scope
7. Scope 2 산정 방식
8. 보고기간과 기준연도
9. 필수 메타데이터 존재 여부

결정 규칙:

```text
필수 메타데이터 누락
→ 정보 부족
→ 계산하지 않음

필드가 존재하지만 범위·단위·방식 불일치
→ 비교 불가
→ 계산하지 않음

모든 필수 조건 일치
→ 비교 가능
→ 대조 엔진 호출
```

`ComparabilityResult`에는 단일 boolean만 저장하지 말고 조건별 결과, 누락 항목, 불일치 이유를 저장한다.

---

## 13. 대조 엔진 규칙

### 실적주장

1. 비교 가능성 검사
2. 비교 불가능하면 즉시 반환
3. 두 값의 표시 정밀도 확인
4. 확인된 표시 규칙 적용
5. exact 또는 precision-compatible 판정
6. 비교 가능한 차이에만 절대 차이와 상대 차이율 계산
7. 차이 설명 근거 재검토
8. 여섯 분석 상태 중 하나와 검토 라우팅 반환

### 표시 정밀도

- `71,840.290`과 정수 `71,840`의 원본 차이 `0.290`은 보존한다.
- 결과가 정밀도 범위에서 양립하면 대표 상태는 `일치`다.
- 보조 문구로 `표시 정밀도 범위 내 정합`을 표시한다.
- 출처의 표시 규칙이 확인되지 않으면 그 한계를 표시한다.
- 임의의 ±1%, ±5% 같은 허용오차를 도입하지 않는다.

### 감축목표

- 기준연도, 목표연도, 기준값, 현재값을 확인한다.
- 실제 감축률은 코드로 계산한다.
- 공개된 연차 경로가 없으면 `on_track`을 만들지 않는다.
- 임의 선형 궤적을 생성하지 않는다.

---

## 14. LLM 실행과 캐시

실행 모드:

| 모드 | 동작 |
| --- | --- |
| `live` | Gemini structured output으로 실제 추출 |
| `demo` | A·B·C의 검수된 구조화 캐시로 전체 흐름 재현 |
| `live 실패 후 cache` | 실패 원인을 기록하고 검증 사례만 cache fallback |

제출물의 기본값은 `demo`다.

캐시 키에는 다음이 포함돼야 한다.

- 문서 해시
- 페이지
- 모델명
- 프롬프트 버전
- 출력 스키마 버전

화면과 트레이스에서 `실시간 추출`, `검증 캐시 사용`, `실시간 실패 후 검증 캐시 사용`을 구분한다.

`demo` 모드에서 캐시가 없는 임의 PDF 분석은 비활성화한다.

---

## 15. 공개 데이터 저장 전략

공식 데모는 검증 저장본을 우선 사용한다.

1. 출처 URL, 조회일, 원본 응답 해시를 저장한다.
2. 데이터 버전을 저장한다.
3. 수동 새로고침 시 새 버전을 추가한다.
4. 이전 값을 덮어쓰지 않는다.
5. 갱신 실패 시 저장본으로 계속 분석한다.
6. 화면에 `저장 데이터 사용 중`과 기준일을 표시한다.
7. 트레이스에 조회 시각, 성공/실패, 사용한 버전을 기록한다.

외부 사이트의 HTML 구조는 바뀔 수 있으므로 live scraping이 핵심 데모를 깨뜨리게 하면 안 된다.

---

## 16. `db/envinfo.py`에 대한 인수 주의

현재 파일은 바로 신뢰하고 확장하면 안 된다.

### 수정이 필요한 표현

현재 모듈 docstring에는 환경정보공개시스템을 `실측 소스`, `법정 신고 실측치`라고 표현한다. 최신 기획과 맞지 않는다.

권장 표현:

> 환경정보공개시스템에 공개된 환경 데이터 수집기. 항목의 의무·자율 여부, 기업·사업장 범위, Scope와 산정 방식, 조회 시점과 버전을 함께 보존하며 다른 출처의 절대적 정답으로 간주하지 않는다.

### 수정이 필요한 데이터 구조

현재 `EnvRecord`에는 최신 `PublicFact` 계약에 필요한 다음 필드가 부족하다.

- `entity_level`
- 조직경계
- 지역경계
- Scope 2 산정 방식
- 표시 정밀도
- 표시 규칙
- 공개 항목의 의무/자율 구분
- 출처 URL
- 조회 시각
- 원본 HTML 해시
- 데이터 버전

### 검증이 필요한 휴리스틱

현재 구현은 Scope 1·2·총량이 모두 0이면 제조사업장의 미입력으로 간주한다. 이 판단을 모든 기업과 업종에 일반화하면 안 된다.

- 원문 페이지가 정말 미입력을 0으로 표시하는지 fixture로 검증한다.
- `0`, 공란, 미공개, 수집 실패를 서로 다른 상태로 저장한다.
- 일반 규칙이 아니라 출처별 명시 규칙 또는 검증된 사례 규칙으로 제한한다.
- `reported_total`을 Scope 1+2로 대체하는 fallback도 Scope 3 포함 여부와 표 구조가 명확한 경우에만 허용한다.
- 광범위한 `except Exception`은 실패 원인을 보존하되 테스트 가능한 예외 유형으로 좁히는 것을 검토한다.

### 보존할 수 있는 부분

- 요청 세션 재사용
- 요청 간 지연
- HTML을 별도 `parse()` 함수로 분리한 구조
- 없는 값을 `None`으로 보존하려는 방향
- 수집 실패를 숨기지 않는 방향

이 파일을 수정할 때는 원본 HTML fixture 기반 파서 테스트를 먼저 만든다.

---

## 17. UI와 표현 규칙

### 핵심 네 장면

1. 사후관리 검토 대기열
2. B의 비교 가능성 검사와 계산 중단 트레이스
3. 주장 카드와 원문·페이지·출처 근거
4. A의 일치, C의 설명되지 않은 차이와 HITL

### 사용 가능한 표현

- 일치
- 표시 정밀도 범위 내 정합
- 설명된 차이
- 설명 가능성 있음
- 설명되지 않은 차이
- 비교 불가
- 정보 부족
- 별도 검토 필요
- 자동 추출 결과 — 원문 확인 필요

### 금지 표현

- 그린워싱 적발
- 허위 판정
- 법 위반 확정
- 법정 실측치와 무조건 불일치
- 위험기업
- 자동 제재

실제 기업에는 강한 불일치를 시연하지 않는다. 강한 불일치는 합성 C로만 보여준다.

---

## 18. HITL

담당자 조치:

- `추가 자료 요청`
- `검토 완료`
- `보류`

필수 저장값:

- 검토자
- 처리 시각
- 조치
- 메모
- 이전 조치
- 변경 후 조치

`추가 자료 요청`을 선택하면 비교 가능성 검사에서 누락되거나 불일치한 조건으로 질문 초안을 만든다.

`검토 완료`는 기업이 문제가 없다는 자동 판정이 아니라 현재 업무 처리가 끝났다는 뜻이다.

---

## 19. 테스트 최소선

### 비교 가능성

- 완전 비교 가능
- 조직경계 불일치
- 지역경계 불일치
- Scope 불일치
- Scope 2 산정 방식 불일치
- 기간 불일치
- 원단위/절대량 불일치
- 필수 정보 누락
- B 사례에서 계산 함수 미호출

### 대조 엔진

- 원본값 완전 일치
- A의 표시 정밀도 기준 일치
- 정밀도 경계값
- 표시 규칙 미확인
- C의 25,000 절대 차이
- C의 25% 상대 차이
- 0 분모 처리
- 비교 불가 입력에서 계산 미실행
- 연차 경로 없는 목표에 `on_track` 미생성

### 통합

- API 키 없이 A·B·C 전체 실행
- live 실패 후 cache fallback
- 공개 데이터 갱신 실패 후 snapshot 사용
- 담당자 조치가 AI 상태를 덮어쓰지 않음
- 담당자 조치 변경 이력 보존
- Beta 실패가 A·B·C에 영향을 주지 않음

---

## 20. 구현 순서

날짜가 아니라 다음 순서로 진행한다.

1. MVP 범위와 판정 언어 확인
2. NaRan 골격과 감탄 재사용 범위 확인
3. 공통 데이터 계약과 샘플 JSON
4. A·B·C 정답지와 fixture
5. DB 모델과 기본 API
6. PDF 파서와 주장 추출기
7. 공개 데이터 저장본과 기업·사업장 매핑
8. 비교 가능성 검사
9. 결정론적 대조 엔진과 목표 추적
10. 오케스트레이터와 트레이스
11. 핵심 UI 네 장면
12. HITL과 감사 이력
13. A·B·C 전체 통합
14. 실패 처리와 재현성
15. 선택 기능 판단
16. 품질 보증과 기능 동결
17. 제출 패키지와 발표 준비

상세 작업과 완료 기준은 최신 `나란_개발계획.md`를 읽는다.

---

## 21. 우선순위

### Must

- A·B·C 정답지와 fixture
- 공통 데이터 계약
- demo 모드
- 주장과 원문 페이지
- 공개 데이터 저장본과 출처
- 비교 가능성 검사
- 결정론적 대조 엔진
- B의 계산 중단
- A의 정밀도 기준 일치
- C의 HITL
- 핵심 네 장면 UI
- 트레이스
- 새 환경 재현

### Should

- live Gemini 추출
- live 실패 후 cache fallback
- 공개 데이터 수동 갱신
- 담당자 조치 감사 이력
- 감축목표 추적 준비도
- 차이 원인 재검토

### Could

- 임의 PDF 업로드 Beta
- 메타데이터 수정 후 재실행
- 추가 기업
- 고급 차트와 애니메이션
- 데모 영상

핵심 흐름이 완성되기 전에 `Could` 기능을 시작하지 않는다.

---

## 22. Codex 작업 규칙

### 작업 시작 전

- 관련 최신 기획과 개발 계획을 읽는다.
- 현재 파일 구조를 확인한다.
- Git 저장소 여부와 변경 상태를 확인한다.
- 기존 사용자 파일을 보존한다.
- 큰 변경 전에 작업 범위와 영향을 짧게 설명한다.

### 구현 중

- 작은 단위로 구현하고 바로 테스트한다.
- 핵심 규칙은 순수 함수와 명시적 타입으로 만든다.
- 실제 데이터 값을 UI에 하드코딩하지 않는다.
- fixture도 DB 적재 경로를 거쳐 사용한다.
- 실패와 fallback을 숨기지 않는다.
- 외부 데이터에는 provenance를 남긴다.
- 스키마를 바꾸면 백엔드 모델, API 응답, 프론트 타입, fixture, 테스트를 함께 갱신한다.
- 감탄 코드의 도메인 문구를 단순 치환해 끝내지 않는다.

### 하지 말 것

- 감탄 저장소 전체 복사
- 감탄 저장소의 작업 중인 파일 수정
- 공개 데이터의 범위를 확인하지 않은 숫자 비교
- LLM의 최종 판정
- 고정 오차율 도입
- 연차 감축 경로 임의 생성
- 실제 기업의 강한 부정 판정
- 합성 여신 정보를 실제 KB 데이터처럼 표시
- API 키나 비밀값 커밋
- 핵심 테스트를 통과하기 전 UI 장식에 시간 사용
- 커밋 메시지에 `Co-Authored-By: Claude` 등 AI 공동저자 표기를 넣는 것. 절대 넣지 않는다.

### 작업 완료 보고

Codex는 다음을 간결하게 보고한다.

- 무엇을 구현했는지
- 어떤 파일이 바뀌었는지
- 어떤 테스트를 실행했고 결과가 어땠는지
- 남은 위험이나 다음 Step이 무엇인지
- fixture, cache, live 데이터 중 무엇을 사용했는지

---

## 23. Codex가 다음으로 할 일

사용자가 개발 시작을 지시하면 곧바로 전체 앱을 만들기 전에 다음 순서로 진행한다.

1. 최신 기획서와 개발 계획을 다시 읽는다.
2. NaRan와 GamTan의 실제 파일 구조를 읽기 전용으로 비교한다.
3. 재사용 후보와 제외 대상을 실제 코드 기준으로 확정한다.
4. 현재 `db/envinfo.py`의 문제를 테스트로 재현하고 최신 `PublicFact` 계약에 맞춘 수정안을 만든다.
5. 공통 Pydantic 모델과 A·B·C 샘플 JSON을 먼저 구현한다.
6. 샘플 계약을 사용자와 팀원이 검토할 수 있게 보여준다.
7. 계약이 고정되면 DB·UI fixture와 파서·판정 엔진을 병렬로 진행한다.

Git 초기화, 감탄 파일 이전, 패키지 설치처럼 저장소 상태를 크게 바꾸는 작업은 사용자의 개발 지시 범위 안에서 수행하되, 대상과 영향을 먼저 명확히 알린다.

---

## 24. 완료 판단

나란 MVP는 기능 개수가 아니라 다음 질문에 코드와 화면으로 답할 수 있을 때 완료다.

1. 보고서의 어떤 문장을 어떤 페이지에서 추출했는가?
2. 어떤 공개 데이터를 언제, 어떤 버전으로 사용했는가?
3. 두 값은 기업·사업장·조직·지역·Scope·방식·기간이 같은가?
4. 다르다면 계산을 정말 중단했는가?
5. 같다면 계산식과 표시 정밀도 근거를 보여주는가?
6. AI 결과와 담당자 조치가 분리돼 있는가?
7. API 키와 네트워크가 없어도 A·B·C가 재현되는가?

위 일곱 항목이 충족되지 않으면 Beta 업로드나 추가 차트가 있어도 MVP 완료로 보지 않는다.
